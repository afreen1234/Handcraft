@using System.Web.Optimization
@using EPiServer.Shell.Navigation
@using EPiServer.Framework.Web.Mvc.Html
@using Handcraft.Models.ViewModels
@using Handcraft.Models.Pages
@using EPiServer.Core
@model  IPageViewModel<SitePageData>

@*@{
    var _contentRepository = EPiServer.ServiceLocation.ServiceLocator.Current.GetInstance<EPiServer.IContentRepository>();
    var childPages = _contentRepository.GetChildren<PageData>(ContentReference.StartPage);
}*@
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=10" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    @Scripts.Render("~/bundles/js");
    @Styles.Render("~/bundles/css");
</head>
<body>
    <div class="container-fluid">
        @if (Model != null)
        {
            Html.RenderPartial("_Header", Model);
        }
        @RenderBody()
        <hr/>
        <div class="footer">
            @if (Model != null)
            {
                Html.RenderPartial("_Footer", Model);
            }
        </div>
    </div>
</body>
</html>